{
  "name": "llm-wiki-liiy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:web": "npm --prefix web run build",
    "test": "vitest run",
    "test:live-llm-wiki-liiy": "vitest run test/runtime/live-llm-wiki-liiy.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "typecheck:web": "npx tsc --noEmit -p web/tsconfig.json",
    "lint": "eslint .",
    "install-hooks": "node scripts/install-git-hooks.mjs",
    "prepare": "npm run install-hooks"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "@types/node": "^24.3.0",
    "eslint": "^9.10.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.10.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@mariozechner/pi-agent-core": "^0.66.1",
    "@mariozechner/pi-ai": "^0.66.1",
    "pdf-parse": "^2.4.5",
    "pg": "^8.16.3",
    "yaml": "^2.8.3"
  }
}
